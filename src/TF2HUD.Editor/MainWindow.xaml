<Window x:Class="TF2HUD.Editor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TF2HUD.Editor.Common"
        mc:Ignorable="d"
        Title="TF2 HUD Editor"
        Height="800" Width="1300"
        FontFamily="../Resources/TF2Secondary.ttf #TF2 Secondary"
        FontSize="20px">

    <Grid Background="#2B2724">

        <Label
            Name="LblStatus"
            Margin="10,652,0,0"
            Content="..." />

        <local:SelectHUD x:Name="SelectHud" Margin="10,10,10,132" />
        <local:FlawHUD x:Name="FlawHud" Visibility="Hidden" Margin="10,10,10,132" />
        <local:rayshud x:Name="RaysHud" Visibility="Hidden" Margin="200,40,200,140" />

        <GroupBox
            Width="1271"
            Height="90"
            Margin="10,684,0,0"
            Header="Buttons">

            <Grid>
                <Button
                    x:Name="BtnInstall"
                    Width="190"
                    Height="40"
                    Click="BtnInstall_OnClick"
                    Content="Install"
                    Style="{StaticResource Button}" Margin="10,10,0,0" />

                <Button
                    x:Name="BtnUninstall"
                    Width="190"
                    Height="40"
                    Click="BtnUninstall_OnClick"
                    Content="Uninstall"
                    Style="{StaticResource Button}" Margin="218,10,0,0" />

                <Button
                    x:Name="BtnSave"
                    Width="190"
                    Height="40"
                    Click="BtnSave_OnClick"
                    Content="Apply Changes"
                    Style="{StaticResource Button}" Margin="426,10,0,0" />

                <Button
                    x:Name="BtnReset"
                    Width="190"
                    Height="40"
                    Click="BtnReset_OnClick"
                    Content="Reset to Default"
                    Style="{StaticResource Button}" Margin="634,10,0,0" />

                <Button
                    x:Name="BtnReportIssue"
                    Width="190"
                    Height="40"
                    Click="BtnReportIssue_OnClick"
                    Content="Report Issue"
                    Style="{StaticResource Button}" Margin="842,10,0,0" />

                <Button
                    x:Name="BtnSwitch"
                    Width="190"
                    Height="40"
                    Click="BtnSwitch_OnClick"
                    Content="Switch HUD"
                    Style="{StaticResource Button}" Margin="1050,10,0,0" />
            </Grid>
        </GroupBox>
    </Grid>
</Window>